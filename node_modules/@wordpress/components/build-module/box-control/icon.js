import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import { createElement } from "@wordpress/element";

/**
 * Internal dependencies
 */
import { Root, Viewbox, TopStroke, RightStroke, BottomStroke, LeftStroke } from './styles/box-control-icon-styles';
var BASE_ICON_SIZE = 24;
export default function BoxControlIcon(_ref) {
  var _ref$size = _ref.size,
      size = _ref$size === void 0 ? 24 : _ref$size,
      _ref$side = _ref.side,
      side = _ref$side === void 0 ? 'all' : _ref$side,
      sides = _ref.sides,
      props = _objectWithoutProperties(_ref, ["size", "side", "sides"]);

  var isSideDisabled = function isSideDisabled(value) {
    return (sides === null || sides === void 0 ? void 0 : sides.length) && !sides.includes(value);
  };

  var getSide = function getSide(value) {
    if (isSideDisabled(value)) {
      return false;
    }

    return side === 'all' || side === value;
  };

  var top = getSide('top');
  var right = getSide('right');
  var bottom = getSide('bottom');
  var left = getSide('left'); // Simulates SVG Icon scaling

  var scale = size / BASE_ICON_SIZE;
  return createElement(Root, _extends({
    style: {
      transform: "scale(".concat(scale, ")")
    }
  }, props), createElement(Viewbox, null, createElement(TopStroke, {
    isFocused: top
  }), createElement(RightStroke, {
    isFocused: right
  }), createElement(BottomStroke, {
    isFocused: bottom
  }), createElement(LeftStroke, {
    isFocused: left
  })));
}
//# sourceMappingURL=icon.js.map