/**
 * WordPress dependencies
 */
import { _x, __ } from '@wordpress/i18n';
/**
 * Internal dependencies
 */

var metadata = {
  apiVersion: 2,
  name: "core/post-navigation-link",
  category: "design",
  attributes: {
    textAlign: {
      type: "string"
    },
    type: {
      type: "string",
      "default": "next"
    },
    label: {
      type: "string"
    },
    showTitle: {
      type: "boolean",
      "default": false
    }
  },
  supports: {
    reusable: false,
    html: false
  }
};
import edit from './edit';
import variations from './variations';
var name = metadata.name;
export { metadata, name };
export var settings = {
  title: _x('Post Navigation Link', 'name of the block that displays the next or previous post link that is adjacent to the current post'),
  description: __('Displays the next or previous post link that is adjacent to the current post.'),
  edit: edit,
  variations: variations
};
//# sourceMappingURL=index.js.map