/**
 * External dependencies
 */

/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { Platform } from '@wordpress/element';
/**
 * Constants
 */

var isWeb = Platform.OS === 'web';
export var PC_WIDTH_DEFAULT = 50;
export var PX_WIDTH_DEFAULT = 350;
export var MIN_WIDTH = 220;
export var MIN_WIDTH_UNIT = 'px';
export var CSS_UNITS = [{
  value: '%',
  label: isWeb ? '%' : __('Percentage (%)'),
  default: PC_WIDTH_DEFAULT
}, {
  value: 'px',
  label: isWeb ? 'px' : __('Pixels (px)'),
  default: PX_WIDTH_DEFAULT
}];
/**
 * Returns a boolean whether passed unit is percentage
 *
 * @param {string} unit Block width unit.
 *
 * @return {boolean} 	Whether unit is '%'.
 */

export function isPercentageUnit(unit) {
  return unit === '%';
}
//# sourceMappingURL=utils.js.map